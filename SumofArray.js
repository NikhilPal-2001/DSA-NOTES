function ArrayQuestion(Arr)
{
    let sum = 0;
    for (let i = 0 ; i <= Arr.length ;i++)
    {
         sum = sum +i ;
        
    }

    console.log (sum);
}
Arr = [10,9,8,7,7,7,5,5]
ArrayQuestion(Arr);
